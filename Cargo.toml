[package]
name = "cuda-prompt"
version = "0.1.0"
edition = "2021"
description = "Prompt engineering primitives — template composition, variable injection, chain-of-thought scaffolding, and prompt optimization"

[dependencies]
serde = { version = "1", features = ["derive"] }
